version: '3'
services:
  nginx-tls-upgrader:
    image: nginx:alpine
    container_name: nginx-tls-upgrader
    ports:
      - "8080:8080"  # WebSocket port
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro  # Mount as main config
    restart: unless-stopped