## Getting started

Running the server locally requires an SSL certificate, the certificates in this directory have been generated with the following command:

```
mkcert -cert-file server.crt -key-file server.key localhost 127.0.0.1
```